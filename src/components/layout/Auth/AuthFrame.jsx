import styles from '../../../styles/components/layout/Auth/AuthFrame.module.css';

const AuthFrame = () => {
    return (
        <div className={styles["AuthFrame-container"]}>
            
        </div>
    );
};

export default AuthFrame;